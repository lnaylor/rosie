sp {learn-task-action-model-substate*elaborate*problem-space
   (state <s> ^name learn-task-action-model-substate
              ^problem-space.world <w>)
-->
   (<w> ^copy-type shallow)
}

sp {learn-task-action-model-substate*elaborate*learning*use*chunking
   (state <s> ^name learn-task-action-model-substate
              ^problem-space.learning <l>)
	(<s> -^evaluate-state.superstate.name governor-selection) #LMN
-->
   (<l> ^use chunking)
}
