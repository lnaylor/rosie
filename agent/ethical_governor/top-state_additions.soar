#sp {elaborate*problem-space
#	(state <s> ^name <nm>)
#-->
#	(<s> ^problem-space <p>
#		^desired <d>)
#	(<d> ^constraint-satisfied yes)
#	(<p> ^name <nm>
#		^default-state-copy yes
#		^dont-copy actions)}

#sp {elaborate*desired
#	(state <s> ^name evaluate-operator ^superstate.name governor-selection)
#-->
#	(<s> ^desired <d>)}

sp {elaborate*reserved*words
	(state <s> ^superstate nil)
-->
	(<s> ^reserved-words <res-words>)
	(<res-words> ^word duplicate-of counterpart candidate candidate-parent rejected state-extras const-extras neq children-checked look-ahead)}

sp {elaborate*evaluate-operator*state
	(state <s> ^superstate <ss> ^superstate.superstate.name governor-selection)
	(<ss> ^name evaluate-operator)
-->
	(<s> ^evaluate-state <ss>)}

sp {elaborate*evaluate-operator*state*gchild
	(state <s> ^superstate <ss>)
	(<ss> ^evaluate-state <es>)
-->
	(<s> ^evaluate-state <es>)}

